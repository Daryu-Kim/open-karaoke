<Window x:Class="OpenKaraoke.App.DownloadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="노래 다운로드"
        Width="620"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource Brush.Bg}"
        Foreground="{StaticResource Brush.TextPrimary}"
        FontFamily="Malgun Gothic"
        UseLayoutRounding="True">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- 입력 필드 -->
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}" />
            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
            <Setter Property="CaretBrush" Value="{StaticResource Brush.AccentCyan}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Brush.AccentCyan}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,16,0,7" />
        </Style>

        <!-- 진행 바 -->
        <Style x:Key="ProgressTrack" TargetType="ProgressBar">
            <Setter Property="Height" Value="10" />
            <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}" />
            <Setter Property="Foreground" Value="{StaticResource Brush.AccentGradient}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <Border Background="{TemplateBinding Background}" CornerRadius="5" />
                            <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}"
                                    CornerRadius="5" HorizontalAlignment="Left" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 제목 -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="노래 다운로드" FontSize="26" FontWeight="Bold" />
            <TextBlock Style="{StaticResource MutedText}" FontSize="14" Margin="0,6,0,0"
                       Text="라이브러리에 저장할 곡 정보를 확인하고 다운로드하세요." />
        </StackPanel>

        <!-- 미리보기 + 입력 -->
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Width="280" Height="158" CornerRadius="12"
                    Background="{StaticResource Brush.SurfaceAlt}" ClipToBounds="True"
                    HorizontalAlignment="Left" VerticalAlignment="Top">
                <Image Source="{Binding ThumbnailUrl}" Stretch="UniformToFill" />
            </Border>

            <StackPanel Grid.Column="1" Margin="20,0,0,0">
                <TextBlock Style="{StaticResource FormLabel}" Text="제목" />
                <TextBox Style="{StaticResource FormTextBox}" Text="{Binding SongTitle, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding CanStart}" />

                <TextBlock Style="{StaticResource FormLabel}" Text="가수" />
                <TextBox Style="{StaticResource FormTextBox}" Text="{Binding Artist, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding CanStart}" />

                <TextBlock Style="{StaticResource FormLabel}" Text="TJ 번호 (선택)" />
                <TextBox Style="{StaticResource FormTextBox}" Text="{Binding TjNumber, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding CanStart}" />
            </StackPanel>
        </Grid>

        <!-- 진행 -->
        <StackPanel Grid.Row="2" Margin="0,22,0,0"
                    Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <ProgressBar Style="{StaticResource ProgressTrack}"
                         Minimum="0" Maximum="100" Value="{Binding ProgressPercent}" />
            <TextBlock Text="{Binding ProgressPercent, StringFormat={}{0:0}%}" FontSize="14"
                       Style="{StaticResource MutedText}" Margin="0,6,0,0" HorizontalAlignment="Right" />
        </StackPanel>

        <!-- 상태 메시지 -->
        <TextBlock Grid.Row="3" Text="{Binding StatusText}" FontSize="15" Margin="0,14,0,0"
                   TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Phase}" Value="Failed">
                            <Setter Property="Foreground" Value="{StaticResource Brush.Danger}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Phase}" Value="Success">
                            <Setter Property="Foreground" Value="{StaticResource Brush.Success}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- 버튼 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Style="{StaticResource PrimaryButton}" Content="다운로드"
                    Command="{Binding StartDownloadCommand}" MinWidth="130" Padding="22,8" />
            <Button Style="{StaticResource GhostButton}" Content="취소" Margin="10,0,0,0"
                    Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</Window>
